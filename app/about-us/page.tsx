import Naviagation from "../../components/navigation";

export default function AboutUs() {
	return (
		<div>
			<h1>About us</h1>
		</div>
	);
}

// you must create a page inside the directory to make the page visible
